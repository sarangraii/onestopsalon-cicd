name: OneStopSalon CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build & Push Backend Image
        run: |
          docker build -t yourname/onestopsalon-backend ./backend
          docker push yourname/onestopsalon-backend

      - name: Build & Push Frontend Image
        run: |
          docker build -t yourname/onestopsalon-frontend ./frontend
          docker push yourname/onestopsalon-frontend
